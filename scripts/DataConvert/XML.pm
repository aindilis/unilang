# # ===== Using an object
# use XML::Dumper;
# $dump = new XML::Dumper;

# $xml  = $dump->pl2xml( $perl );
# $perl = $dump->xml2pl( $xml );
# $dump->pl2xml( $perl, "my_perl_data.xml.gz" );

# # ===== Using function calls
# use XML::Dumper;

# $xml  = pl2xml( $perl );
# $perl = xml2pl( $xml );
